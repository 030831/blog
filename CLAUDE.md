# 개인 블로그 (030831)

이 파일은 새 Claude Code 세션이 프로젝트 맥락을 바로 파악하기 위한 문서입니다.
노트북과 데스크톱을 오가며 작업하므로, 결정 사항은 대화가 아니라 **여기에** 기록합니다.

## 이 프로젝트가 무엇인가

- 백엔드(Java/Spring/MySQL)를 공부하며 남기는 개인 기술 블로그 겸 포트폴리오
- 저장소: https://github.com/030831/blog
- 배포: https://030831-blog.vercel.app (main에 push하면 Vercel이 자동 배포)
- 로컬 경로: 노트북 `C:\myblog` / 데스크톱은 클론한 위치

## 스택과 그 이유

**Astro + 마크다운 정적 사이트.** DB 없음. 서버 없음.

- 글 하나 = `src/content/posts/` 안의 `.md` 파일 하나. Git이 사실상 DB 역할
- 빌드 시점에 HTML로 구워 CDN에 올림 → 요청 시 애플리케이션/DB 관여 없음
- React/Next 대신 Astro를 쓴 이유: 결과물이 거의 순수 HTML/CSS라 프론트 지식 없이도
  유지보수가 가능하고, 마크다운이 표준 형식이라 나중에 다른 프레임워크로 이전 가능
- 조회수·댓글이 필요해지면 그때 Spring Boot + MySQL API를 별도로 붙이는 방향
  (블로그 본문은 정적으로 유지)

## 작업자에 대해

사용자는 **백엔드 학습자**입니다. 프론트엔드·CSS·디자인은 잘 모르며,
그 영역의 결정은 Claude가 맡아서 해주기를 원합니다. 선택지를 나열해 되묻지 말고
근거를 갖춘 결과물을 제시할 것.

다만 **기술 스택 선택은 신중하게** 다룹니다. 스택을 제안할 때는
(1) 검토한 대안과 그것을 택하지 않은 구체적 이유, (2) 나중에 빠져나올 수 있는 경로를
함께 설명해야 납득합니다.

## 구조

```
src/
  config.ts            사이트 전역 설정 — 제목, 주소, 메뉴, 카테고리 표시 이름
  content/posts/       글. 폴더 구조가 곧 카테고리 트리
  content.config.ts    프론트매터 스키마 (오타는 빌드에서 잡힘)
  lib/posts.ts         글 목록, 슬러그, 카테고리 경로, 읽는 시간
  lib/categories.ts    카테고리 트리 생성, 브레드크럼
  layouts/             BaseLayout(공통) / SidebarLayout(카테고리 사이드바용)
  components/          SiteHeader(헤더+검색), PostCard, SearchResults,
                       CategoryTree, Breadcrumb, Footer 등
  pages/               라우팅. 파일 = 주소
  styles/global.css    구조 + 본문 스타일
  styles/skins.css     색 팔레트 (warm 한 벌. 라이트/다크)
  pages/search-index.json.ts  검색용 색인 생성
astro.config.ts        코드 블록(Shiki), 제목 앵커, 표 감싸기 설정
```

## 반드시 지켜야 할 규칙

**글의 URL은 프론트매터의 `postId`(글 번호)로 정해집니다** (`lib/posts.ts`의 `slugOf`).
`/posts/12/` 형태입니다. 파일 이름·제목·카테고리를 나중에 바꿔도 링크가 살아 있습니다.

번호를 쓰는 이유: 한글 제목을 주소에 그대로 넣으면 `%EC%BB%A4...` 로 인코딩되어
읽을 수 없고, 로마자로 옮기면(`teuraenjaeksyeon-...`) 읽기도 어려운 데다 검색엔진이
원래 낱말로 인식하지도 못합니다. 글쓴이에게 매번 영문 주소를 입력시키는 것도
불필요한 마찰이라 판단해 번호로 정했습니다.

번호는 편집기와 `npm run new` 가 **남아 있는 글들의 최대값 + 1** 로 매깁니다.
가장 최근 글을 지우면 그 번호는 다시 쓰입니다(공유된 링크가 있었다면 주의).
번호가 겹치면 빌드가 실패합니다(의도된 동작). **사람이 손으로 고치지 말 것.**

**프론트매터의 날짜는 UTC 자정으로 해석됩니다.** 한국 시간 기준 오늘 쓴 글이
미래 글로 분류되어 빌드에서 빠지는 문제가 있어, `getPublishedPosts`에서
시각이 아니라 날짜 단위로 비교합니다. 이 비교 로직을 단순화하지 말 것.

**파일 이름은 주소와 무관합니다.** 사람이 알아보기 위한 것이라 자유롭게 바꿔도 됩니다.
기본값은 제목을 로마자로 옮긴 것(`src/lib/slug.js`)이며, 한글 파일명을 쓰지 않는 이유는
주소 때문이 아니라 macOS/Windows 사이의 유니코드 정규화 차이로 Git에서 같은 파일이
두 개로 보이는 문제를 피하기 위해서입니다.

## 자주 쓰는 명령

```bash
npm run dev              # 개발 서버 (http://localhost:4321)
npm run build            # 정적 빌드
npm run write            # 편집기를 열고 브라우저에서 글쓰기 (권장)
npm run new "글 제목" --slug english-name --in backend/spring
```

## 확정된 디자인

상단 중앙 정렬 헤더 + 테두리 카드 목록. justicehui.github.io의 외형을 참고했으나
CSS와 페이지 제목은 `.page-title` 클래스로 글자 뒤에만 형광펜처럼 배경을 깝니다.
카테고리 트리처럼 경계가 필요한 영역은 `.panel`(테두리 박스)로 감쌉니다.

인터랙션은 새로 작성했습니다.

```
              030831                  워드마크 (큰 글자 + 강조선)
        백엔드를 공부하며 남기는 기록   한 줄 소개
  ─────────────────────────────────────────────  얇은 선
   홈  글  카테고리        [검색  제목▾]  ☀      도구 막대
  ─────────────────────────────────────────────
  ┌───────────────────────────────────────────┐
  │ 카테고리                                   │  작은 라벨(강조색)
  │ 글 제목                                    │  1.4rem / 700
  │ 발췌문 두 줄                                │
  │ ─────────────────────────────────────────  │
  │ 2026년 9월 3일              더 읽어보기 →   │
  └───────────────────────────────────────────┘
```

헤더는 **머리말(이름+소개)** 과 **도구 막대(이동+검색)** 두 덩어리뿐입니다.
아이콘 내비, 카테고리 칩을 쌓아 올렸다가 위계 없는 위젯 더미가 되어 걷어냈습니다.
헤더에 요소를 더 얹지 말 것 — 본문이 시작되기까지의 높이를 170px 안쪽으로 유지합니다.

카테고리 칩은 홈에서 보이지 않게 해달라는 요청으로 완전히 제거했습니다.
카테고리 이동은 도구 막대의 '카테고리' 메뉴가 담당합니다.

### 사이트 이름 (워드마크)

이름이 숫자라 **상자에 담으면 라벨로만 보입니다.** 검은 둥근 사각형에 넣었다가
"디자인이 별로"라는 지적을 받고 글자 자체를 크게 세우는 방식으로 바꿨습니다.
2.15rem / 800 / tabular-nums 에 아래 강조선을 두고, 호버 시 선이 이름 폭까지 자랍니다.
이 강조선은 도구 막대의 현재 위치 표시와 같은 언어입니다.
**다시 상자에 넣지 말 것.**

페이지 폭은 전부 `max-width: 46rem` 가운데 정렬입니다. 사이드바 레이아웃은 쓰지 않습니다
(본문이 오른쪽으로 치우쳐 보이는 문제가 있었습니다).

목록의 두 가지 형태:
- **카드**(`PostCard`) — 홈에서만. 제목·날짜·카테고리·발췌·더 읽어보기 버튼
- **행**(`PostRow`) — 글 목록/카테고리/태그. 제목과 날짜만

카테고리 페이지(`/categories/`)는 **카테고리 트리만** 보여줍니다. 글은 넣지 않습니다.
개별 카테고리로 들어가야 글이 행 형태로 나옵니다.

트리(`CategoryTree`)는 **가로 흐름 격자**이고 **모든 항목이 버튼**입니다.
버튼 바탕은 카드와 같은 `--surface`(라이트=흰색). 검은 채움은 너무 무겁다는 지적을 받았습니다.
최상위와 하위는 색이 아니라 **크기와 굵기**로 구분합니다(1rem/700 vs .9rem/일반).
테두리는 완전한 알약(999px).
최상위만 맨 글자로 두면 일관성이 깨진다는 지적을 받았습니다.
바깥 테두리 박스도 트리를 가두기만 해서 제거했습니다. 왼쪽 열에 최상위 카테고리를 이름순으로
세로 정렬하고, 오른쪽에 하위를 가로로 흘려 넘치면 다음 줄로 접습니다.
깊이는 들여쓰기가 아니라 `Spring › JPA › 영속성 컨텍스트` 같은 경로 표기로 드러내고,
앞쪽 조각을 흐리게 처리해 마지막 이름이 눈에 들어오게 합니다.

이 형태로 두 번 바뀌었습니다. 카드 격자는 빈 공간만 커졌고, 세로 들여쓰기 트리는
깊어질수록 오른쪽으로 밀리고 세로로 길어져서 버렸습니다.
**들여쓰기로 계층을 표현하는 방식으로 되돌리지 말 것.**

### 움직임 설정 (중요)

`prefers-reduced-motion: reduce`를 존중하되, **모든 전환을 없애지는 않습니다.**
어지럼증을 유발하는 것은 위치 이동이지 색 변화가 아니기 때문입니다.

`global.css`에서 transition-property를 색·배경·테두리·그림자·투명도로 제한하고,
각 컴포넌트의 reduced-motion 블록에서는 hover 시 `transform`만 none으로 되돌립니다.
**컴포넌트에서 `transition: none`을 쓰지 말 것** — 그러면 색 전환까지 죽어서
"hover는 되는데 아무 효과가 없다"는 상태가 됩니다. 실제로 그 문제가 있었습니다.

Windows의 '애니메이션 효과'가 꺼져 있으면 이 설정이 켜집니다. 개발 중 애니메이션이
안 보인다면 먼저 이걸 의심할 것.

## 글 편집기

`npm run write` 로 개발 서버를 띄우면 헤더에 **글쓰기** 링크가 생깁니다.
(주소로 직접 가려면 http://localhost:4321/editor)

`astro dev --open /editor` 처럼 경로를 넘기면 Git Bash가 `/editor` 를 윈도우 경로로
바꿔버려 오류가 납니다. 그래서 `--open` 만 쓰고 링크로 안내합니다.

velog 같은 좌우 분할 편집기입니다. 왼쪽에 마크다운을 쓰면 오른쪽에 바로 그려지고,
저장하면 `src/content/posts/` 아래에 `.md` 파일이 생깁니다.

**개발 서버에서만 열립니다.** 파일을 디스크에 쓰려면 서버가 필요한데 배포본은
정적 파일뿐입니다. 그래서 `src/pages` 밖(`src/dev/`)에 두고
`astro.config.ts` 의 `devEditor()` 통합이 `command === 'dev'` 일 때만 라우트를 주입합니다.
**빌드 결과물에 편집기가 들어가면 안 됩니다** — 누구나 글을 쓸 수 있게 됩니다.

- 임시저장 → `draft: true`, 출간하기 → `draft: false`
- 어느 쪽이든 파일만 만듭니다. **실제 배포는 git push 를 해야 합니다**
- 쓰던 글은 localStorage 에도 자동 보관되어 새로고침해도 남습니다

## 페이징

쪽당 개수는 두 목록이 다릅니다. 카드는 자리를 많이 차지하고 행은 적게 차지하기 때문입니다.

- 홈: `SITE.homePostsPerPage` (현재 5) — 카드 형태
- 글 목록: `SITE.postsPerPage` (현재 10) — 제목+날짜 행

- 1쪽은 `/`, `/posts/` 그대로. 2쪽부터 `/page/2/`, `/posts/page/2/`
- 이 구조를 쓴 이유: `/posts/2` 로 하면 글 상세 라우트(`[...slug]`)와 경로가 겹칩니다
- 쪽 번호는 처음·끝·현재 주변만 남기고 `…`로 접어 버튼 줄이 한 줄을 넘지 않게 합니다
- 갈 곳 없는 `<` `>`는 숨기지 않고 흐리게 둡니다. 사라지면 버튼 줄이 좌우로 흔들립니다
- 카테고리 페이지는 아직 페이징하지 않습니다. 한 카테고리에 글이 수십 개가 되면 그때 붙일 것

**샘플 글이 들어 있습니다.** `src/content/posts/sample/` 의 20개는 페이징을 눈으로
확인하려고 넣은 더미입니다. 실제 글을 쓰기 시작하면 폴더째 지우고
`src/config.ts` 의 `sample: '샘플'` 줄도 함께 지우세요.

## 검색

정적 사이트라 서버가 없으므로 **빌드 시점에 `/search-index.json`을 구워두고
브라우저가 받아서 직접 거릅니다** (`src/pages/search-index.json.ts`).

- 범위 세 가지: 제목 / 제목+내용 / 카테고리
- 색인에서 코드 블록은 제외합니다. `public`, `return` 같은 흔한 키워드에
  모든 글이 걸려 검색이 무의미해지기 때문입니다
- 검색 UI는 헤더에 항상 있지만, 결과를 그릴 `#search-results`가 있는 페이지
  (홈·글 목록·카테고리)에서만 즉시 검색됩니다. 그 외 페이지에서는 Enter 시
  `/?q=...&scope=...`로 이동합니다
- 글이 수백 개가 되어 색인이 무거워지면 body를 잘라 담거나 색인을 분할할 것

## 하지 않기로 한 것

- 댓글, 조회수, 회원 기능 — 당장 필요 없다고 확인됨
- **태그 페이지와 소개 페이지** — 사용자 요청으로 삭제. 프론트매터의 `tags`는
  검색 색인과 RSS에 남아 있어 검색 키워드로는 계속 동작합니다.
  다시 필요하면 `/tags` 라우트만 만들면 됩니다
- 티스토리/velog 같은 서비스형 플랫폼 — 글 이전이 어려워서 배제
